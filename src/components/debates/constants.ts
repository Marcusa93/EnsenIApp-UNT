/**
 * Constantes compartidas entre el composer (cliente) y las Server Actions.
 * Este módulo NO lleva "use client": importarlo desde el servidor debe dar
 * el valor real, no una referencia de cliente.
 */
export const ARGUMENT_MAX_LENGTH = 4000;
